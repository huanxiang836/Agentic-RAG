{
  "profile": "smoke",
  "sample_count": 12,
  "metrics": {
    "context_recall": 0.7569444444444445,
    "faithfulness": null
  },
  "rows": [
    {
      "userInput": "请从职责边界、协作方式和适用场景三个角度，比较 A2A 和 Multi-agent 的差异。",
      "reference": "A2A 更强调跨系统、跨框架的标准通信与调用；Multi-agent 更强调同一应用内多个 Agent 的分工协作；前者偏对外互联，后者偏内部编排；实际选择取决于你是要连接外部 Agent，还是组织内部专家链路。",
      "response": "A2A（Agent-to-Agent）协议与 Multi-agent（多智能体）架构虽然都涉及多个 Agent 的协同，但它们的关注点和解决的问题层面不同。以下是基于职责边界、协作方式和适用场景的详细对比：\n\n### 1. 职责边界 (Responsibility Boundaries)\n\n*   **Multi-agent：侧重于“功能与逻辑”的拆分**\n    *   **边界定义**：在一个应用系统内部，根据任务的专业领域或工作流步骤来划分边界。\n    *   **具体表现**：每个 Agent 被赋予特定的角色（如“规划器”、“研究员”、“代码执行器”）。其核心目的是解决单个 Agent 能力不足的问题（如上下文窗口限制、工具选择困难），通过分工合作来完成复杂任务。\n*   **A2A：侧重于“系统与组织”的隔离**\n    *   **边界定义**：以独立的 Agent 实体为边界，通常对应不同的系统、服务、技术栈甚至不同的公司。\n    *   **具体表现**：A2A 协议关注的是如何让两个完全独立的 Agent 实体进行交互。它不关心对方内部是如何工作的（对方内部可能是单 Agent 也可能是 Multi-agent），只关心如何建立连接和交换数据。\n\n### 2. 协作方式 (Collaboration Methods)\n\n*   **Multi-agent：框架内的编排与流转**\n    *   **机制**：通常依赖于特定的开发框架（如 Spring AI, LangChain 等）提供的内部机制。\n    *   **模式**：\n        *   **Tool Calling**：Supervisor Agent 将其他 Agent 视为“工具”进行调用，控制权高度集中。\n        *   **Handoffs**：当前 Agent 根据对话状态将控制权移交给下一个 Agent，控制权在 Agent 间动态流转。\n*   **A2A：协议级的远程通信**\n    *   **机制**：基于标准化的网络协议（如 HTTP/JSON），类似于微服务架构中的 RPC 调用。\n    *   **模式**：\n        *   **发现**：通过 `AgentCard`（类似 API 文档）在注册中心（如 Nacos）发现远程 Agent 的能力。\n        *   **调用**：通过标准的远程接口（Invoke）发起请求，获取结果。它屏蔽了底层框架的差异，实现了跨框架的互操作。\n\n### 3. 适用场景 (Applicable Scenarios)\n\n*   **Multi-agent：单应用内的复杂任务处理**\n    *   **场景**：当你需要构建一个功能强大的单一应用，且该应用需要处理多步骤、多领域的复杂逻辑时。\n    *   **例子**：一个“旅行规划助手”，内部包含“搜索 Agent\"负责查攻略，“计算 Agent\"负责算预算，“绘图 Agent\"负责生成路线图。\n*   **A2A：跨系统、跨团队的集成与复用**\n    *   **场景**：当企业内存在多个独立开发的 Agent 系统，或者需要复用外部/第三方的 Agent 能力时。\n    *   **例子**：你的公司内部有一个基于 Spring AI 开发的“客服 Agent\"，需要调用外部合作伙伴基于 LangChain 开发的“物流查询 Agent\"。此时 A2A 协议就是连接两者的桥梁。\n\n### 总结\n\n*   **Multi-agent** 解决的是“**如何在一个系统内把事做对**”（分工、编排、降低单点复杂度）。\n*   **A2A** 解决的是“**如何让不同的系统互相听懂**”（标准化通信、跨框架互操作、分布式集成）。\n\n在实际架构中，两者往往是结合的：一个 **Multi-agent** 系统内部，可以通过 **A2A** 协议去调用外部的远程 Agent 能力。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\multi-agent.md\n内容: **Multi-agent** 将复杂的应用程序分解为多个协同工作的专业化Agent。与依赖单个Agent处理所有步骤不同， **Multi-agent架构** 允许你将更小、更专注的Agent组合成协调的工作流。  \nMulti-agent系统在以下情况下很有用：  \n- 单个Agent拥有太多工具，难以做出正确的工具选择决策\n- 上下文或记忆增长过大，单个Agent难以有效跟踪\n- 任务需要 **专业化** （例如：规划器、研究员、数学专家）",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: **Multi-agent** 将复杂的应用程序分解为多个协同工作的专业化Agent。与依赖单个Agent处理所有步骤不同， **Multi-agent架构** 允许你将更小、更专注的Agent组合成协调的工作流。  \nMulti-agent系统在以下情况下很有用：  \n- 单个Agent拥有太多工具，难以做出正确的工具选择决策\n- 上下文或记忆增长过大，单个Agent难以有效跟踪\n- 任务需要 **专业化** （例如：规划器、研究员、数学专家）",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\overview.md\n内容: ## 设计原则  \n我们推荐您使用 Agent Framework 内置的 `ReactAgent` 抽象快速构建 Agent 应用。  \n如果您需要组合 Multi-agent，Agent Framework 还预置了如 `SequentialAgent（顺序代理）` 、 `ParallelAgent（并行代理）` 、 `RoutingAgent（路由代理）` 和 `LoopAgent（循环代理）` 等基础多智能体工作流模式。  \n对于一些开发场景而言，直接使用 `Graph API` 也是可行的，它能为应用开发提供更灵活的编排、更直接的状态控制，适用于需要超高可靠性、大量自定义逻辑、需要精确控制延迟时的场景。"
      ],
      "metricScores": {
        "context_recall": 0.25,
        "faithfulness": null
      }
    },
    {
      "userInput": "A2A 架构里 Server、Registry、Discovery 各自承担什么职责，三者如何配合？",
      "reference": "A2A Server 负责把本地 ReactAgent 暴露成可调用服务；Registry 负责登记和保存 AgentCard；Discovery 负责按需发现可用 Agent；三者组合起来形成注册、发现、调用的闭环。",
      "response": "在 A2A（Agent-to-Agent）架构中，Server、Registry 和 Discovery 是构建分布式 Agent 协作网络的核心组件。它们各自承担不同的职责，并通过“注册 - 发现”机制紧密配合，实现 Agent 之间的解耦与动态通信。\n\n### 1. 各自职责\n\n*   **A2A Server（服务端/提供者）**\n    *   **核心职责**：承载具体的 Agent 逻辑（如 ReactAgent），并将其能力暴露为标准的 A2A 服务。\n    *   **关键动作**：它维护着描述自身能力的元数据（即 **AgentCard**，包含名称、描述、接口定义等），并负责将这些信息发布出去。\n*   **A2A Registry（注册中心）**\n    *   **核心职责**：作为 Agent 的“通讯录”或“黄页”，负责存储和管理所有可用的 Agent 信息。\n    *   **关键动作**：接收 Server 的注册请求，持久化存储 AgentCard，并向订阅者推送变更。通常由 Nacos 等组件实现。\n*   **A2A Discovery（发现机制）**\n    *   **核心职责**：负责让 Agent 消费者（Client）能够找到可用的服务提供者。\n    *   **关键动作**：从 Registry 中拉取或订阅最新的 AgentCard 列表，实现动态的服务发现，并通常支持负载均衡策略，以便 Client 选择最合适的 Server 进行调用。\n\n### 2. 三者如何配合\n\n这三者的配合流程类似于微服务架构中的服务治理，主要分为以下三个阶段：\n\n1.  **服务注册（Server $\\rightarrow$ Registry）**：\n    当 A2A Server 启动时，它会读取本地 Agent 的配置（如 `server.card.name` 等），构建出 AgentCard，并将其注册到 **A2A Registry** 中。这相当于 Agent 在“通讯录”上登记了自己的信息。\n2.  **服务发现（Discovery $\\rightarrow$ Registry）**：\n    当需要调用其他 Agent 时，**A2A Discovery** 组件会向 Registry 发起查询或订阅请求。Registry 返回当前可用的 AgentCard 列表。Discovery 组件会缓存这些信息，并在 Agent 上下线时保持同步。\n3.  **服务调用（Client $\\rightarrow$ Server）**：\n    基于 Discovery 获取到的 AgentCard 信息，Client（消费者）可以知道目标 Agent 的地址和能力，从而发起远程调用。Discovery 机制在此过程中通常还会介入，根据负载均衡策略选择一个具体的 Server 实例进行请求转发。\n\n**总结**：\nServer 是能力的提供者，Registry 是信息的存储库，Discovery 是连接的桥梁。三者结合，实现了 Agent 之间的解耦，使得 Agent 网络能够动态扩展、自动发现并高可用地运行。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## A2A 架构  \nSpring AI Alibaba 的 A2A 实现包含三个核心组件：  \n1. **A2A Server** ：将本地 ReactAgent 暴露为 A2A 服务\n2. **A2A Registry** ：Agent 注册中心（支持 Nacos）\n3. **A2A Discovery** ：Agent 发现机制（支持 Nacos）",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ### 配置 Nacos Registry 和 Discovery  \n**Nacos A2A 配置示例**  \n```\nspring:\nai:\nalibaba:\na2a:\nnacos:\nserver-addr: 127.0.0.1:8848\nusername: nacos\npassword: nacos\ndiscovery:\nenabled: true   # 启用服务发现（查询其他 Agent）\nregistry:\nenabled: true   # 启用服务注册（注册本地 Agent）\nserver:\nversion: 1.0.0\ncard:\nname: data_analysis_agent\ndescription: 专门用于数据分析和统计计算的本地智能体\nprovider:\nname: Spring AI Alibaba Documentation\norganization: Spring AI Alibaba\n```  \n**重要配置说明** ：  \n- `registry.enabled: true` - 必须启用才能将 Agent 注册到 Nacos（服务提供者）\n- `discovery.enabled: true` - 启用后才能通过 AgentCardProvider 发现其他 Agent（服务消费者）\n- `server.card.name` - 必须与 ReactAgent Bean 的 `name` 一致",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## 基于 Nacos 的 A2A Registry  \nNacos3 最新版本提供了 A2A AgentCard 模型的存储与推送支持，因此可以作为 A2A Registry 实现。Spring AI Alibaba 通过与 Nacos A2A Registry 集成，可以实现：  \n1. **A2A Server AgentCard 自动注册到 Nacos A2A Registry**\n2. **A2a Client 自动订阅发现可用 AgentCard，实现 AgentCard 调用的负载均衡**"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "发布一个 A2A 智能体时，最少要完成哪些配置，哪些是可选项？",
      "reference": "最少要先创建 ReactAgent Bean，再配置 A2A Server；如果需要自动注册和动态发现，再补充 Nacos Registry 和 Discovery；也就是说，发布是必需，注册与发现是增强能力。",
      "response": "发布 A2A 智能体时，配置主要分为**必填（核心）**和**可选（扩展）**两部分：\n\n### 1. 最少必填配置（核心）\n要让智能体作为 A2A 服务运行并对外暴露接口，必须完成以下两项：\n\n* **代码层：创建 `ReactAgent` Bean**\n  必须在应用中定义并注册一个 `ReactAgent` 类型的 Bean，这是智能体的核心逻辑载体。\n* **配置层：A2A Server 基础元数据**\n  在 `application.yml` 中必须配置 A2A Server 的基础信息。系统启动后会自动根据 Bean 生成 `AgentCard` 并暴露 REST API。必填字段包括：\n  * `spring.ai.alibaba.a2a.server.version`：服务版本号。\n  * `spring.ai.alibaba.a2a.server.card.name`：智能体名称（**必须与 `ReactAgent` Bean 的 `name` 保持一致**）。\n  * `spring.ai.alibaba.a2a.server.card.description`：智能体功能描述。\n  * `spring.ai.alibaba.a2a.server.provider.name`：提供者名称。\n  * `spring.ai.alibaba.a2a.server.provider.organization`：提供者组织/机构名称。\n\n### 2. 可选项配置（扩展）\n* **Nacos 注册与发现**\n  如果你的部署环境不需要通过 Nacos 进行服务注册或发现其他 Agent，则**完全不需要**配置此项。\n  若需要使用 Nacos 进行服务治理，则需补充以下配置：\n  * **连接信息**：`server-addr`、`username`、`password`。\n  * **功能开关**：\n    * `discovery.enabled`：是否启用服务发现（用于查询/调用其他 Agent）。\n    * `registry.enabled`：是否启用服务注册（将当前 Agent 注册到 Nacos，供其他服务发现）。\n\n**总结**：只要完成 `ReactAgent` Bean 的创建和 A2A Server 的元数据配置，智能体即可独立发布，并自动通过 `/.well-known/agent.json`（元数据）和 `/a2a/message`（调用端点）对外提供服务。Nacos 相关配置仅在微服务架构需要服务注册/发现时才作为可选项添加。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## 发布 A2A 智能体  \n要将一个智能体发布为 A2A 服务，需要：  \n1. 创建 ReactAgent Bean\n2. 配置 A2A Server\n3. （可选）配置 Nacos Registry 进行自动注册",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ### 配置 Nacos Registry 和 Discovery  \n**Nacos A2A 配置示例**  \n```\nspring:\nai:\nalibaba:\na2a:\nnacos:\nserver-addr: 127.0.0.1:8848\nusername: nacos\npassword: nacos\ndiscovery:\nenabled: true   # 启用服务发现（查询其他 Agent）\nregistry:\nenabled: true   # 启用服务注册（注册本地 Agent）\nserver:\nversion: 1.0.0\ncard:\nname: data_analysis_agent\ndescription: 专门用于数据分析和统计计算的本地智能体\nprovider:\nname: Spring AI Alibaba Documentation\norganization: Spring AI Alibaba\n```  \n**重要配置说明** ：  \n- `registry.enabled: true` - 必须启用才能将 Agent 注册到 Nacos（服务提供者）\n- `discovery.enabled: true` - 启用后才能通过 AgentCardProvider 发现其他 Agent（服务消费者）\n- `server.card.name` - 必须与 ReactAgent Bean 的 `name` 一致",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ### 配置 A2A Server  \n在 `application.yml` 中配置 A2A Server：  \n**A2A Server 配置示例**  \n```\nspring:\nai:\nalibaba:\na2a:\nserver:\nversion: 1.0.0\ncard:\nname: data_analysis_agent\ndescription: 专门用于数据分析和统计计算的本地智能体\nprovider:\nname: Spring AI Alibaba Documentation\norganization: Spring AI Alibaba\n```  \n启动应用后，A2A Server 会自动：  \n- 根据 ReactAgent Bean 生成 AgentCard\n- 暴露 REST API 端点：\n- `/.well-known/agent.json` - AgentCard 元数据\n- `/a2a/message` - Agent 调用端点"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "在配置 A2A Server 和 Nacos 时，哪些字段最容易配错，为什么？",
      "reference": "最容易配错的是 ReactAgent Bean 的 name、server.card.name 以及 Nacos 连接信息；因为这些字段直接决定 AgentCard 是否能正确生成、注册和被发现。",
      "response": "在配置 A2A Server 和 Nacos 时，最容易配错的字段通常涉及**功能开关**、**元数据一致性**以及**连接认证**。以下是具体的易错点及原因分析：\n\n### 1. 功能开关字段：`registry.enabled` 和 `discovery.enabled`\n*   **易错表现**：忘记配置或误设为 `false`。\n*   **原因**：\n    *   **`registry.enabled`**：控制 Agent 是否作为服务提供者注册到 Nacos。如果不设为 `true`，你的 Agent 将无法被其他服务发现。\n    *   **`discovery.enabled`**：控制 Agent 是否作为服务消费者去发现其他 Agent。如果不设为 `true`，你的 Agent 将无法通过 `AgentCardProvider` 查询到网络中的其他智能体。\n    *   这两个字段是功能生效的前提，一旦遗漏，整个 A2A 通信机制就会失效。\n\n### 2. 元数据一致性字段：`server.card.name`\n*   **易错表现**：配置的名称与代码中定义的 Agent 名称不一致。\n*   **原因**：\n    *   A2A Server 启动时会自动生成 AgentCard 元数据。该字段的值**必须与代码中 `ReactAgent` Bean 的 `name` 属性完全一致**。\n    *   如果两者不匹配，会导致生成的元数据与实际运行的 Agent 无法对应，进而引发调用失败或识别错误。\n\n### 3. 连接认证字段：`server-addr`, `username`, `password`\n*   **易错表现**：地址端口错误、使用了错误的默认密码。\n*   **原因**：\n    *   这是最基础的连接配置。如果 Nacos 服务部署在非默认端口（如非 8848），或者 Nacos 开启了鉴权且修改了默认密码（默认通常是 nacos/nacos），而配置文件未同步更新，会导致 Agent 无法连接到 Nacos 服务器。\n    *   排查时通常需要查看应用日志，确认 Nacos Registry 的自动配置是否生效以及连接是否被拒绝。\n\n### 4. 架构约束：多 Agent 注册\n*   **易错表现**：试图在单个应用实例的配置中注册多个 Agent。\n*   **原因**：\n    *   默认机制下，一个应用实例通常只注册一个 Agent Bean。\n    *   如果你需要注册多个 Agent，通常的做法是运行**多个应用实例**，每个实例配置不同的 Agent，而不是在一个配置文件中试图定义多个。\n\n### 5. 依赖缺失（非字段但关键）\n*   **易错表现**：项目中未引入 `spring-ai-alibaba-starter-a2a-nacos` 依赖。\n*   **原因**：\n    *   如果没有这个依赖，相关的自动配置类（AutoConfiguration）根本不会加载，导致你在 YAML 中配置的所有字段都无效。\n\n**建议**：在配置完成后，务必检查应用启动日志，确认 Nacos 注册成功，并验证 `/.well-known/agent.json` 端点返回的元数据是否正确。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ### 配置 Nacos Registry 和 Discovery  \n**Nacos A2A 配置示例**  \n```\nspring:\nai:\nalibaba:\na2a:\nnacos:\nserver-addr: 127.0.0.1:8848\nusername: nacos\npassword: nacos\ndiscovery:\nenabled: true   # 启用服务发现（查询其他 Agent）\nregistry:\nenabled: true   # 启用服务注册（注册本地 Agent）\nserver:\nversion: 1.0.0\ncard:\nname: data_analysis_agent\ndescription: 专门用于数据分析和统计计算的本地智能体\nprovider:\nname: Spring AI Alibaba Documentation\norganization: Spring AI Alibaba\n```  \n**重要配置说明** ：  \n- `registry.enabled: true` - 必须启用才能将 Agent 注册到 Nacos（服务提供者）\n- `discovery.enabled: true` - 启用后才能通过 AgentCardProvider 发现其他 Agent（服务消费者）\n- `server.card.name` - 必须与 ReactAgent Bean 的 `name` 一致",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## 故障排查  \n### Agent 没有注册到 Nacos  \n- 检查 `registry.enabled: true` 是否配置\n- 查看应用日志，确认 Nacos Registry AutoConfiguration 是否生效\n- 验证 Nacos 连接配置（server-addr、username、password）",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## 注意事项  \n1. **依赖要求** ：\n- 需要添加 `spring-ai-alibaba-starter-a2a-nacos` 依赖\n- 确保 Nacos 服务正常运行\n2. **AgentCard 元数据** ：\n- `server.card.name` 必须与 ReactAgent Bean 的 `name` 一致\n- `server.card.provider` 可选，用于标识 Agent 提供者信息\n3. **多 Agent 注册** ：\n- 默认情况下，只有一个 Agent Bean 会被注册\n- 如需注册多个 Agent，需运行多个应用实例，每个实例配置不同的 Agent"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "如果要用 AgentCardProvider 调用远程 Agent，整体链路应该怎么理解？",
      "reference": "先从注册中心获取目标 AgentCard，再拿到远程 Agent 的地址和能力描述，最后按 A2A 协议发起调用；本质上它把发现和调用拆成了两个明确步骤。",
      "response": "使用 `AgentCardProvider` 调用远程 Agent 的整体链路可以概括为：**注册 -> 发现 -> 构建 -> 调用**。\n\n这一机制的核心在于通过注册中心（如 Nacos）解耦了调用方与被调用方，使得远程 Agent 的调用方式与本地 Agent 保持一致。具体链路理解如下：\n\n### 1. 核心链路步骤\n\n1.  **服务注册（前提）**\n    远程 Agent 在启动时，会将其能力描述（即 `AgentCard`，包含名称、描述、接口地址等信息）发布到注册中心（如 Nacos）。\n\n2.  **服务发现（Discovery）**\n    调用方通过注入 `AgentCardProvider`，向注册中心发起查询。`AgentCardProvider` 负责根据 Agent 的名称获取对应的 `AgentCard` 元数据。\n\n3.  **构建代理（Build）**\n    使用 `A2aRemoteAgent.builder()` 构建一个远程代理对象。\n    *   在此步骤中，你需要指定目标 Agent 的 `name` 和 `agentCardProvider`。\n    *   框架内部会利用 `AgentCardProvider` 获取到的元数据，初始化远程通信所需的配置。\n\n4.  **发起调用（Invoke）**\n    调用 `A2aRemoteAgent` 的 `invoke(input)` 方法。\n    *   框架底层会根据 `AgentCard` 中的地址和协议，将请求发送到远程 Agent。\n    *   远程 Agent 处理完毕后，将结果（`OverAllState`）返回给调用方。\n\n### 2. 关键组件作用\n\n*   **`AgentCardProvider`**：充当“黄页”查询器。它不直接参与通信，只负责从注册中心获取远程 Agent 的元数据（AgentCard）。\n*   **`A2aRemoteAgent`**：充当“通信代理”。它封装了远程调用的细节，对外暴露与本地 Agent 一致的 `invoke` 接口，实现了“远程调用本地化”。\n\n### 3. 代码示例解析\n\n```java\n// 1. 注入 Provider (服务发现组件)\n@Autowired\nprivate AgentCardProvider agentCardProvider;\n\npublic void callRemoteAgent() {\n    // 2. 构建远程代理 (组装阶段)\n    // 框架会通过 agentCardProvider 自动去 Nacos 找 \"data_analysis_agent\" 的卡片\n    A2aRemoteAgent remote = A2aRemoteAgent.builder()\n            .name(\"data_analysis_agent\")\n            .agentCardProvider(agentCardProvider) \n            .description(\"数据分析远程代理\")\n            .build();\n\n    // 3. 执行调用 (通信阶段)\n    // 此时发生实际的远程网络请求\n    Optional<OverAllState> result = remote.invoke(\"请根据季度数据给出分析。\");\n    \n    result.ifPresent(state -> System.out.println(\"结果：\" + state.value(\"output\")));\n}\n```\n\n**总结**：理解这个链路的关键在于明白 `AgentCardProvider` 只是负责**拿地址（元数据）**，而 `A2aRemoteAgent` 才是负责**打电话（执行请求）**的组件。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## 调用 A2A 远程智能体  \n### 使用 AgentCardProvider 发现 Agent  \nSpring AI Alibaba 支持通过 `AgentCardProvider` 从注册中心（如 Nacos）发现远程 Agent。  \n**使用 AgentCardProvider 发现并调用远程 Agent**  \n```\nimport com.alibaba.cloud.ai.graph.agent.a2a.A2aRemoteAgent;\nimport com.alibaba.cloud.ai.graph.agent.a2a.AgentCardProvider;\nimport com.alibaba.cloud.ai.graph.OverAllState;\nimport org.springframework.beans.factory.annotation.Autowired;\nimport org.springframework.stereotype.Component;\n\nimport java.util.Optional;\n\n@Component\npublic class A2AExample {\n\nprivate final AgentCardProvider agentCardProvider;\n\n@Autowired\npublic A2AExample(AgentCardProvider agentCardProvider) {\nthis.agentCardProvider = agentCardProvider;\n}\n\npublic void callRemoteAgent() {\n// 通过 AgentCardProvider 从注册中心发现 Agent\nA2aRemoteAgent remote = A2aRemoteAgent.builder()\n.name(\"data_analysis_agent\")\n.agentCardProvider(agentCardProvider)  // 从 Nacos 自动获取 AgentCard\n.description(\"数据分析远程代理\")\n.build();\n\n// 远程调用\nOptional<OverAllState> result = remote.invoke(\"请根据季度数据给出同比与环比分析概要。\");\n\nresult.ifPresent(state -> {\nSystem.out.println(\"调用成功: \" + state.value(\"output\"));\n});\n}\n}\n```",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: public void runDemo() {\n// 1. 本地直连：验证本地注册的 ReactAgent 可用\nOptional<OverAllState> localResult = localDataAnalysisAgent.invoke(\n\"请对上月销售数据进行趋势分析，并给出关键结论。\"\n);\nlocalResult.ifPresent(state -> {\nSystem.out.println(\"本地调用成功\");\n});\n\n// 2. 发现：通过 AgentCardProvider 从注册中心获取该 Agent 的 AgentCard\nA2aRemoteAgent remote = A2aRemoteAgent.builder()\n.name(\"data_analysis_agent\")\n.agentCardProvider(agentCardProvider)  // 从 Nacos 自动获取 AgentCard\n.description(\"数据分析远程代理\")\n.build();\n\n// 3. 远程调用：通过 A2aRemoteAgent 调用\nOptional<OverAllState> remoteResult = remote.invoke(\n\"请根据季度数据给出同比与环比分析概要。\"\n);\nremoteResult.ifPresent(state -> {\nSystem.out.println(\"远程调用成功\");\n});\n}\n}\n```",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ### 完整示例  \n**A2A 一体化示例：注册 -&gt; 发现 -&gt; 调用**  \n```\nimport com.alibaba.cloud.ai.graph.agent.ReactAgent;\nimport com.alibaba.cloud.ai.graph.agent.a2a.A2aRemoteAgent;\nimport com.alibaba.cloud.ai.graph.agent.a2a.AgentCardProvider;\nimport com.alibaba.cloud.ai.graph.OverAllState;\nimport org.springframework.ai.chat.model.ChatModel;\nimport org.springframework.beans.factory.annotation.Autowired;\nimport org.springframework.beans.factory.annotation.Qualifier;\nimport org.springframework.stereotype.Component;\n\nimport java.util.Optional;\n\n@Component\npublic class A2AExample {\n\nprivate final ChatModel chatModel;\nprivate final AgentCardProvider agentCardProvider;\nprivate final ReactAgent localDataAnalysisAgent;\n\n@Autowired\npublic A2AExample(@Qualifier(\"dashscopeChatModel\") ChatModel chatModel,\nAgentCardProvider agentCardProvider,\n@Qualifier(\"dataAnalysisAgent\") ReactAgent localDataAnalysisAgent) {\nthis.chatModel = chatModel;\nthis.agentCardProvider = agentCardProvider;\nthis.localDataAnalysisAgent = localDataAnalysisAgent;\n}"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "请比较 Tool Calling 和 Handoffs 两种多 Agent 模式，分别适合什么任务？",
      "reference": "Tool Calling 适合集中式编排，由主 Agent 统一决定何时调用子 Agent；Handoffs 适合把当前会话控制权直接交给更合适的专家 Agent；前者偏工作流，后者偏角色接管。",
      "response": "Tool Calling（工具调用）和 Handoffs（交接）是构建多 Agent 系统时两种核心的协作模式。它们的核心区别在于**控制流**（谁在指挥）和**用户交互方式**（用户跟谁说话）。\n\n以下是详细的对比与适用场景分析：\n\n### 1. Tool Calling（工具调用）模式\n在这种模式下，系统采用**集中式**控制。有一个“控制器”（Supervisor Agent）作为大脑，它将其他 Agent 视为“工具”（AgentTool）。\n\n*   **工作机制**：控制器接收用户输入，决定调用哪个子 Agent 来执行特定任务。子 Agent 执行完毕后，将结果返回给控制器，由控制器汇总并回复用户。\n*   **交互特点**：子 Agent 是“幕后工作者”，**不期望与用户继续对话**。用户只与控制器交互。\n*   **适合的任务**：\n    *   **任务编排与结构化工作流**：适合步骤明确、需要按顺序或并行处理的任务。\n    *   **示例**：比如“旅行规划助手”。控制器收到指令后，依次调用“机票查询 Agent\"、“酒店搜索 Agent\"和“天气查询 Agent\"，最后由控制器将整合好的行程单发给用户。用户不需要直接跟查机票的 Agent 聊天。\n\n### 2. Handoffs（交接）模式\n在这种模式下，系统采用**去中心化**控制。控制权可以在不同的 Agent 之间动态转移。\n\n*   **工作机制**：当前的 Agent 在判断自己无法处理或需要更专业的知识时，会将控制权移交给另一个 Agent。\n*   **交互特点**：控制权转移后，**用户直接与新的 Agent 进行交互**。就像客服系统里的“转接电话”。\n*   **适合的任务**：\n    *   **跨领域对话与专家接管**：适合对话场景复杂、需要不同领域专家介入的情况。\n    *   **示例**：比如“通用客服 Agent\"在处理用户咨询时，发现用户问的是复杂的代码报错问题，于是将控制权 Handoff 给“技术专家 Agent\"。此时用户直接与技术专家 Agent 对话，直到问题解决。\n\n### 总结对比\n\n| 特性 | Tool Calling (工具调用) | Handoffs (交接) |\n| :--- | :--- | :--- |\n| **控制流** | **集中式**：所有路由通过 Supervisor | **去中心化**：控制权动态转移 |\n| **用户交互** | 用户只与 Supervisor 交互 | 用户与当前活跃的 Agent 交互 |\n| **子 Agent 角色** | 执行任务的“工具” | 接管的“专家” |\n| **最佳场景** | 自动化流水线、后台数据处理、结构化任务 | 多轮对话、复杂问题转接、跨领域咨询 |\n\n**一句话记忆**：\n*   **Tool Calling** 是“**老板派活**”（老板指挥员工干活，员工不直接见客户）。\n*   **Handoffs** 是“**转接电话**”（前台接不住，直接把电话转给技术专家，客户直接跟专家聊）。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ## 工具调用（Tool Calling）  \n在 **工具调用** 中，一个Agent（\" **控制器** \"）将其他Agent视为 *工具* （AgentTool），在需要时调用。控制器管理编排，而工具Agent执行特定任务并返回结果。  \n流程：  \n1. **控制器** 接收输入并决定调用哪个工具（子Agent）\n2. **工具Agent** 根据控制器的指令运行其任务\n3. **工具Agent** 将结果返回给控制器\n4. **控制器** 决定下一步或完成任务  \nagent tool  \n作为工具使用的Agent通常 **不期望** 与用户继续对话。它们的角色是执行任务并将结果返回给控制器Agent。如果你需要子Agent能够与用户对话，请改用**交接（HandOff）**模式。  \n关于工具调用模式的使用请查看 [HandOffs 模式文档](https://java2ai.com/docs/frameworks/agent-framework/advanced/multi-agent) 。",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ## Multi-agent模式  \nSpring AI Alibaba支持以下Multi-agent模式：  \n| 模式           | 工作原理                                                            | 控制流                           | 使用场景         |\n|--------------|-----------------------------------------------------------------|-------------------------------|--------------|\n| Tool Calling | Supervisor Agent将其他Agent作为工具调用。\"工具\"Agent不直接与用户对话——它们只执行任务并返回结果。 | 集中式：所有路由都通过调用Agent。           | 任务编排、结构化工作流。 |\n| Handoffs     | 当前的Agent决定将控制权转移给另一个Agent。活动Agent随之变更，用户可以继续与新的Agent直接交互。       | 去中心化：Agent可以改变当前由谁来担当活跃Agent。 | 跨领域对话、专家接管。  |",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\multi-agent.md\n内容: ## Multi-agent模式  \nSpring AI Alibaba支持以下Multi-agent模式：  \n| 模式           | 工作原理                                                            | 控制流                           | 使用场景         |\n|--------------|-----------------------------------------------------------------|-------------------------------|--------------|\n| Tool Calling | Supervisor Agent将其他Agent作为工具调用。\"工具\"Agent不直接与用户对话——它们只执行任务并返回结果。 | 集中式：所有路由都通过调用Agent。           | 任务编排、结构化工作流。 |\n| Handoffs     | 当前的Agent决定将控制权转移给另一个Agent。活动Agent随之变更，用户可以继续与新的Agent直接交互。       | 去中心化：Agent可以改变当前由谁来担当活跃Agent。 | 跨领域对话、专家接管。  |"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "从路由、编排和扩展性三个角度，Multi-agent 常见模式应该怎么选？",
      "reference": "如果任务需要明确编排和结果汇总，优先 Tool Calling；如果需要连续对话中切换专家，优先 Handoffs；如果任务分发要靠输入语义自动决定，优先 LlmRoutingAgent。",
      "response": "选择 Multi-agent（多智能体）架构模式时，通常需要在**路由策略**（如何决定谁执行）、**编排方式**（如何控制执行流）和**扩展性**（如何适应业务增长）之间做权衡。\n\n基于常见的 Agent 框架设计，以下是针对这三个维度的模式选择建议：\n\n### 1. 路由角度 (Routing)\n路由决定了“下一个动作由谁执行”以及“控制权如何流转”。\n\n*   **集中式路由 (Supervisor / Tool Calling)**\n    *   **模式**：由一个 Supervisor Agent 统一调度，将其他 Agent 视为工具调用。\n    *   **适用场景**：任务高度结构化，需要统一管控和全局视野。\n    *   **特点**：路由逻辑集中在 Supervisor，便于监控和调试，但 Supervisor 可能成为性能瓶颈。\n*   **去中心化路由 (Handoffs)**\n    *   **模式**：当前 Agent 根据上下文决定将控制权移交给下一个 Agent（如从“客服”转交“技术支持”）。\n    *   **适用场景**：跨领域对话、专家接管。用户感知是同一个助手，但背后是不同专家在接力。\n    *   **特点**：灵活性高，适合处理多轮、多主题的复杂对话。\n*   **条件路由 (RoutingAgent)**\n    *   **模式**：根据输入特征（如意图分类）将请求分发到不同的处理分支。\n    *   **适用场景**：输入类型明确且差异大，需要分流处理的场景。\n*   **静态路由 (Sequential / Parallel)**\n    *   **模式**：路由是硬编码的，按顺序或并行执行。\n    *   **适用场景**：流程固定、无需动态决策的流水线任务。\n\n### 2. 编排角度 (Orchestration)\n编排决定了 Agent 之间的执行顺序和依赖关系。\n\n*   **线性与并行编排 (Sequential / Parallel)**\n    *   **模式**：`SequentialAgent`（A->B->C）或 `ParallelAgent`（A, B, C 同时执行）。\n    *   **适用场景**：简单的数据处理流水线、独立任务的批量处理。\n    *   **特点**：实现简单，延迟可控，但缺乏应对异常或动态变化的灵活性。\n*   **循环编排 (LoopAgent)**\n    *   **模式**：Agent 循环执行直到满足特定条件（如代码生成后的自我修正、反思）。\n    *   **适用场景**：需要迭代优化、自我纠错的任务。\n    *   **特点**：能显著提升输出质量，但需防止死循环。\n*   **图结构编排 (Graph API)**\n    *   **模式**：基于有向无环图（DAG）或复杂图结构定义节点和边。\n    *   **适用场景**：超高可靠性要求、大量自定义逻辑、需要精确控制延迟的复杂业务。\n    *   **特点**：提供最高的编排自由度，支持任意复杂的控制流，但开发和维护成本最高。\n\n### 3. 扩展性角度 (Scalability)\n扩展性关注系统在面对新需求、新 Agent 加入时的适应能力。\n\n*   **工具化扩展 (Tool Calling)**\n    *   **优势**：新增 Agent 只需注册为工具，对现有架构侵入小。\n    *   **劣势**：随着工具增多，Supervisor 的上下文窗口可能成为瓶颈，路由决策变慢。\n*   **模块化扩展 (Handoffs)**\n    *   **优势**：易于扩展新领域的专家 Agent，各 Agent 职责单一，维护成本低。\n    *   **劣势**：跨 Agent 的状态传递和上下文管理可能变得复杂。\n*   **声明式/图扩展 (Graph API)**\n    *   **优势**：适合大规模、长期演进的系统。通过修改图结构即可调整业务流，逻辑解耦。\n    *   **劣势**：初期设计复杂，需要较强的架构设计能力。\n\n### 总结建议\n\n| 需求特征 | 推荐模式 | 理由 |\n| :--- | :--- | :--- |\n| **简单流水线** | `Sequential` / `Parallel` | 实现最快，资源消耗低。 |\n| **需要分流** | `RoutingAgent` | 专注意图识别与分发。 |\n| **需要迭代优化** | `LoopAgent` | 支持自我反思与修正。 |\n| **复杂对话/多领域** | `Handoffs` | 专家接管，用户体验好。 |\n| **复杂任务调度** | `Tool Calling` (Supervisor) | 集中管控，结构化强。 |\n| **极高灵活性/可靠性** | `Graph API` | 支持任意复杂逻辑，控制力最强。 |\n\n**一般路径**：建议从简单的 `Sequential` 或 `Tool Calling` 开始，随着业务逻辑变复杂（如出现循环、分支、多领域对话），再逐步演进到 `Graph API` 或混合模式。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\overview.md\n内容: ## 设计原则  \n我们推荐您使用 Agent Framework 内置的 `ReactAgent` 抽象快速构建 Agent 应用。  \n如果您需要组合 Multi-agent，Agent Framework 还预置了如 `SequentialAgent（顺序代理）` 、 `ParallelAgent（并行代理）` 、 `RoutingAgent（路由代理）` 和 `LoopAgent（循环代理）` 等基础多智能体工作流模式。  \n对于一些开发场景而言，直接使用 `Graph API` 也是可行的，它能为应用开发提供更灵活的编排、更直接的状态控制，适用于需要超高可靠性、大量自定义逻辑、需要精确控制延迟时的场景。",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\multi-agent.md\n内容: ## Multi-agent模式  \nSpring AI Alibaba支持以下Multi-agent模式：  \n| 模式           | 工作原理                                                            | 控制流                           | 使用场景         |\n|--------------|-----------------------------------------------------------------|-------------------------------|--------------|\n| Tool Calling | Supervisor Agent将其他Agent作为工具调用。\"工具\"Agent不直接与用户对话——它们只执行任务并返回结果。 | 集中式：所有路由都通过调用Agent。           | 任务编排、结构化工作流。 |\n| Handoffs     | 当前的Agent决定将控制权转移给另一个Agent。活动Agent随之变更，用户可以继续与新的Agent直接交互。       | 去中心化：Agent可以改变当前由谁来担当活跃Agent。 | 跨领域对话、专家接管。  |",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ## Multi-agent模式  \nSpring AI Alibaba支持以下Multi-agent模式：  \n| 模式           | 工作原理                                                            | 控制流                           | 使用场景         |\n|--------------|-----------------------------------------------------------------|-------------------------------|--------------|\n| Tool Calling | Supervisor Agent将其他Agent作为工具调用。\"工具\"Agent不直接与用户对话——它们只执行任务并返回结果。 | 集中式：所有路由都通过调用Agent。           | 任务编排、结构化工作流。 |\n| Handoffs     | 当前的Agent决定将控制权转移给另一个Agent。活动Agent随之变更，用户可以继续与新的Agent直接交互。       | 去中心化：Agent可以改变当前由谁来担当活跃Agent。 | 跨领域对话、专家接管。  |"
      ],
      "metricScores": {
        "context_recall": 0.6666666666666666,
        "faithfulness": null
      }
    },
    {
      "userInput": "为什么说 Multi-agent 的核心不是多模型堆叠，而是上下文工程？",
      "reference": "因为真正决定系统质量的不是 Agent 数量，而是每个 Agent 看到了什么、忽略了什么、以及如何接收和返回结构化信息；上下文工程把协作边界、职责边界和信息边界说清楚了。",
      "response": "Multi-agent（多智能体）系统的核心之所以是**上下文工程**（Context Engineering）而非简单的**多模型堆叠**，是因为多智能体系统的难点不在于拥有多少个模型，而在于如何让这些模型**高效、准确地协同工作**。\n\n简单来说，模型堆叠只是增加了“工人”的数量，而上下文工程决定了这些工人之间如何“沟通”和“交接”。具体原因如下：\n\n### 1. 决定信息的可见性（谁能看到什么）\n在多智能体系统中，不同的 Agent 负责不同的任务。上下文工程的核心任务是精细控制每个 Agent 能够获取的信息。\n*   **避免信息过载**：不是所有 Agent 都需要知道整个对话的全貌，过多的无关信息会干扰模型判断，导致幻觉。\n*   **传递关键状态**：确保执行任务的 Agent 能拿到前序步骤产生的关键数据或状态，保证任务的连贯性。\n\n### 2. 定制化的指令与角色分配\n上下文工程允许为不同的子 Agent 定制专门的提示词（Prompt）。\n*   例如，一个负责搜索的 Agent 和一个负责写代码的 Agent，虽然底层模型可能相同，但通过上下文工程注入的指令不同，它们的行为模式就会截然不同。这种“因材施教”是系统发挥最大效能的关键。\n\n### 3. 控制交互格式与中间推理\nAgent 之间的协作需要标准化的接口，上下文工程决定了：\n*   **输入/输出格式**：确保 Agent A 的输出能被 Agent B 正确解析，实现无缝衔接。\n*   **中间推理的包含/排除**：有时需要让下游 Agent 看到上游的思考过程（Chain of Thought）以辅助决策，有时则需要隐藏以保持简洁。\n\n### 4. 系统质量的决定性因素\n单纯堆叠模型（增加 Agent 数量或使用更强的模型）并不能自动解决协作问题。如果上下文管理混乱，多模型系统反而会导致：\n*   **幻觉增加**：模型接收到矛盾或无关的上下文。\n*   **效率低下**：重复处理信息或陷入死循环。\n*   **成本飙升**：无效的 Token 消耗。\n\n**总结**\n多模型堆叠解决的是“能力”问题（能不能做），而上下文工程解决的是“协作”问题（怎么做对）。在多智能体架构中，**上下文工程是连接各个智能体的“神经系统”**，它决定了系统整体的可靠性、性能和最终产出质量。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ## 自定义Agent上下文  \nMulti-agent设计的核心是 **上下文工程** ——决定每个Agent看到什么信息。Spring AI Alibaba 为你提供细粒度的控制：  \n- 将对话或状态的哪些部分传递给每个Agent\n- 为子Agent定制专门的提示\n- 包含/排除中间推理\n- 为每个Agent自定义输入/输出格式  \n系统的质量 **在很大程度上取决于** 上下文工程。目标是确保每个Agent都能访问执行任务所需的正确数据，无论它是作为工具还是作为活动Agent。",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\multi-agent.md\n内容: ## 自定义Agent上下文  \nMulti-agent设计的核心是 **上下文工程** ——决定每个Agent看到什么信息。Spring AI Alibaba 为你提供细粒度的控制：  \n- 将对话或状态的哪些部分传递给每个Agent\n- 为子Agent定制专门的提示\n- 包含/排除中间推理\n- 为每个Agent自定义输入/输出格式  \n系统的质量 **在很大程度上取决于** 上下文工程。目标是确保每个Agent都能访问执行任务所需的正确数据，无论它是作为工具还是作为活动Agent。",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: **Multi-agent** 将复杂的应用程序分解为多个协同工作的专业化Agent。与依赖单个Agent处理所有步骤不同， **Multi-agent架构** 允许你将更小、更专注的Agent组合成协调的工作流。  \nMulti-agent系统在以下情况下很有用：  \n- 单个Agent拥有太多工具，难以做出正确的工具选择决策\n- 上下文或记忆增长过大，单个Agent难以有效跟踪\n- 任务需要 **专业化** （例如：规划器、研究员、数学专家）"
      ],
      "metricScores": {
        "context_recall": 0.5,
        "faithfulness": null
      }
    },
    {
      "userInput": "在 Agent Tool 场景里，主 Agent 和子 Agent 的输入输出应该如何约束，才能让协作稳定？",
      "reference": "主 Agent 需要通过提示词和工具描述明确何时调用子 Agent；子 Agent 的输入应尽量用 inputSchema 或 inputType 结构化；输出则用 outputSchema 或 outputType 固定格式，避免主 Agent 解析歧义。",
      "response": "在 Agent Tool 场景下，主 Agent 与子 Agent 的协作稳定性主要依赖于**明确的接口契约**（Schema）和**精准的提示词引导**（Prompt）。以下是具体的约束策略和最佳实践：\n\n### 1. 输入约束：确保“传得对”\n主 Agent 传递给子 Agent 的信息必须清晰、结构化，避免模糊指令导致子 Agent 跑偏。\n\n*   **利用工具元数据**（Metadata）\n    *   **Name & Description**： 为子 Agent 定义极具描述性的名称和描述。主 Agent 依赖这些元数据来决定何时调用该工具。\n    *   *示例：* 不要只写 `description: \"写文章\"`，应写 `description: \"根据给定的主题和字数要求，撰写结构完整的散文或说明文。\"`\n*   **定义结构化输入**（Input Schema）\n    *   使用 `inputSchema` 或 `inputType` 强制规定输入参数的格式（如 JSON Schema）。这能确保主 Agent 在调用时提取并传递了所有必要的上下文信息，而不是随意丢一句话。\n\n### 2. 输出约束：确保“收得准”\n子 Agent 的返回结果必须能被主 Agent 直接解析或理解，避免信息丢失或格式混乱。\n\n*   **定义结构化输出**（Output Schema）\n    *   使用 `outputSchema` 或 `outputType` 约束子 Agent 的返回格式。主 Agent 可以据此直接提取关键字段，而不需要去解析大段文本。\n*   **提示词中的“最终输出”约束**：\n    *   **关键痛点**： 子 Agent 经常在执行完内部工具调用或推理后，忘记将结果返回给主 Agent。\n    *   **解决策略**： 在子 Agent 的 Instruction 中明确强调：“**你必须在最终回复中包含所有关键结果和细节，因为主 Agent 和用户只能看到你最后的输出。**”\n\n### 3. 稳定性保障：错误处理与状态隔离\n除了输入输出，系统的健壮性同样重要。\n\n*   **错误处理机制**：\n    *   在调用子 Agent 的节点中必须包含错误处理逻辑（如捕获异常、重试机制或降级策略）。如果子 Agent 崩溃，不应导致整个主流程中断。\n*   **状态隔离**：\n    *   确保子 Agent 的运行状态（如中间变量、临时记忆）不会污染主 Agent 的全局状态。如果子 Agent 需要独立状态，应使用状态隔离模式或转换节点。\n*   **独立测试**：\n    *   在集成前，务必对每个子 Agent 进行独立测试，验证其在不同输入下的输出是否符合预期 Schema。\n\n**总结**：\n稳定的协作 = **主 Agent 的精准路由**（靠 Description） + **结构化的数据交换**（靠 Schema） + **子 Agent 的明确指令**（靠 Prompt 强调最终输出）。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\multi-agent.md\n内容: ## 交接（Handoffs）  \n💡 **重要参数说明** ：在多 Agent 模式下，以下参数对于控制 Agent 行为和上下文传递至关重要：  \n1. **`instruction`** ：用于在当前 Agent 节点处插入新的问题说明，引导模型和流程运行。支持使用占位符（如 `{input}` 、 `{outputKey}` 等）来动态引用状态中的数据，实现 Agent 之间的数据传递。\n2. **`returnReasoningContent`** ：控制子 Agent 的上下文是否返回父流程中。如果设置为 `false` ，则其他 Agent 不会有机会看到这个子 Agent 内部的推理过程，它们只能看到这个 Agent 输出的内容（比如通过 `outputKey` 引用）。这对于减少上下文大小、提高效率非常有用。\n3. **`includeContents`** ：父流程中可能包含非常多子 Agent 的推理过程、每个子 Agent 的输出等。 `includeContents` 用来控制当前子 Agent 执行时，是只基于自己的 `instruction` 给到的内容工作，还是会带上所有父流程的上下文。设置为 `false` 可以让子 Agent 专注于自己的任务，不受父流程复杂上下文的影响。\n4. **`outputKey`** ：指定输出内容的键名，可被后续 Agent 通过占位符引用（如 `{outputKey}` ）。使用有意义的 `outputKey` 名称，便于后续 Agent 引用和状态管理。\n5. **`systemPrompt`** **和** **`instruction`** **（Routing 和 Supervisor）** ： `LlmRoutingAgent` 和 `SupervisorAgent` 还支持定制 `systemPrompt` 和 `instruction` ，用于覆盖默认实现，控制后续任务流转的行为。 `systemPrompt` 定义路由决策的整体框架， `instruction` 提供具体的路由指导。  \n在 **交接** 模式中，Agent可以直接将控制权传递给彼此。\"活动\"Agent会发生变化，用户与当前拥有控制权的Agent进行交互。  \n流程：  \n1. **当前Agent** 决定它需要另一个Agent的帮助\n2. 它将控制权（和状态）传递给 **下一个Agent**\n3. **新Agent** 直接与用户交互，直到它决定再次交接或完成",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ### 控制子Agent的输入  \n有两个主要杠杆来控制主Agent传递给子Agent的输入：  \n- **修改提示词** ——调整主Agent的提示或工具元数据（即子Agent的名称和描述），以更好地指导何时以及如何调用子Agent。\n- **上下文注入** ——通过使用 `inputSchema` 或 `inputType` 来定义结构化输入，使子Agent能够接收更丰富的上下文信息。",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ### 多个子Agent作为工具  \n在实际应用中，主Agent通常需要访问多个不同的子Agent工具，根据任务需求选择合适的工具进行调用。这种模式允许你构建更灵活、更强大的多Agent系统。  \n**多个子Agent作为工具示例**  \n```\nimport com.alibaba.cloud.ai.graph.agent.ReactAgent;\nimport com.alibaba.cloud.ai.graph.agent.AgentTool;\n\n// 创建写作Agent\nReactAgent writerAgent = ReactAgent.builder()\n.name(\"writer_agent\")\n.model(chatModel)\n.description(\"专门负责创作文章和内容生成\")\n.instruction(\"你是一个专业作家，擅长各类文章创作。\")\n.build();\n\n// 创建翻译Agent\nReactAgent translatorAgent = ReactAgent.builder()\n.name(\"translator_agent\")\n.model(chatModel)\n.description(\"专门负责文本翻译工作\")\n.instruction(\"你是一个专业翻译，能够准确翻译多种语言。\")\n.build();\n\n// 创建总结Agent\nReactAgent summarizerAgent = ReactAgent.builder()\n.name(\"summarizer_agent\")\n.model(chatModel)\n.description(\"专门负责内容总结和提炼\")\n.instruction(\"你是一个内容总结专家，擅长提炼关键信息。\")\n.build();\n\n// 创建主Agent，集成多个工具\nReactAgent multiToolAgent = ReactAgent.builder()\n.name(\"multi_tool_coordinator\")\n.model(chatModel)\n.instruction(\"你可以访问多个专业工具：写作、翻译和总结。\" +\n\"根据用户需求选择合适的工具来完成任务。\")\n.tools(\nAgentTool.getFunctionToolCallback(writerAgent),\nAgentTool.getFunctionToolCallback(translatorAgent),\nAgentTool.getFunctionToolCallback(summarizerAgent)\n)\n.build();"
      ],
      "metricScores": {
        "context_recall": 0.6666666666666666,
        "faithfulness": null
      }
    },
    {
      "userInput": "Tool Calling 里，一个子 Agent 作为工具被调用时，职责边界应如何划分？",
      "reference": "主 Agent 负责理解意图、选择工具和整合结果；子 Agent 负责在自己的专业范围内完成具体任务；子 Agent 不应该替主 Agent 做全局编排，否则职责会混乱。",
      "response": "在 Tool Calling（工具调用）模式下，子 Agent 作为工具被调用时，其职责边界应遵循“**执行者而非决策者**”的原则。具体的划分标准如下：\n\n### 1. 交互边界：不直接面向用户\n*   **子 Agent 职责**：仅接收主 Agent（控制器）的指令，执行任务并返回结果。它**不期望**也不应该直接与最终用户进行对话。\n*   **主 Agent 职责**：负责与用户交互，理解用户意图，并决定何时调用哪个子 Agent。\n*   **注意**：如果你需要子 Agent 能够直接与用户进行多轮对话或处理复杂的交互，应改用 **HandOff**（交接） 模式，而不是将其作为工具调用。\n\n### 2. 任务边界：单一职责与专业化\n*   **子 Agent 职责**：专注于特定的领域或单一类型的任务（例如：专门负责代码生成、专门负责文档翻译、或专门负责数据库查询）。\n*   **主 Agent 职责**：负责将复杂的用户需求拆解，并路由给最合适的子 Agent。\n*   **最佳实践**：为子 Agent 提供清晰、准确的 `description`（描述），以便主 Agent 能准确判断何时调用它。\n\n### 3. 控制边界：无流程控制权\n*   **子 Agent 职责**：作为“黑盒”执行任务。它不知道也不关心整个工作流的下一步是什么，只负责完成当前分配的任务。\n*   **主 Agent 职责**：掌握全局控制权，决定调用顺序、处理异常以及整合多个子 Agent 的结果。\n\n### 4. 状态边界：短生命周期\n*   **子 Agent 职责**：通常表现为无状态或短状态。任务完成即返回结果，不维护长期的对话记忆（除非是任务执行所需的临时上下文）。\n*   **主 Agent 职责**：维护与用户的长期对话上下文和状态。\n\n### 总结对比\n\n| 维度 | 子 Agent (作为 Tool) | 主 Agent (Controller) |\n| :--- | :--- | :--- |\n| **核心角色** | 执行者 (Worker) | 编排者 (Orchestrator) |\n| **用户交互** | ❌ 无 (仅与主 Agent 交互) | ✅ 有 (直接面对用户) |\n| **决策权** | ❌ 无 (按指令执行) | ✅ 有 (决定调用谁、何时调用) |\n| **专注点** | 特定领域的单一任务 | 全局流程与意图理解 |\n\n**一句话总结**： 子 Agent 作为工具时，应该像一个“**功能强大的函数**”——给它输入，它给你输出，中间不插嘴，不抢控制权。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: ## 工具调用（Tool Calling）  \n在 **工具调用** 中，一个Agent（\" **控制器** \"）将其他Agent视为 *工具* （AgentTool），在需要时调用。控制器管理编排，而工具Agent执行特定任务并返回结果。  \n流程：  \n1. **控制器** 接收输入并决定调用哪个工具（子Agent）\n2. **工具Agent** 根据控制器的指令运行其任务\n3. **工具Agent** 将结果返回给控制器\n4. **控制器** 决定下一步或完成任务  \nagent tool  \n作为工具使用的Agent通常 **不期望** 与用户继续对话。它们的角色是执行任务并将结果返回给控制器Agent。如果你需要子Agent能够与用户对话，请改用**交接（HandOff）**模式。  \n关于工具调用模式的使用请查看 [HandOffs 模式文档](https://java2ai.com/docs/frameworks/agent-framework/advanced/multi-agent) 。",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\multi-agent.md\n内容: ## 选择模式  \n| 问题              | 工具调用 (Agent Tool)   | 交接（Handoffs）   |\n|-----------------|---------------------|----------------|\n| 需要集中控制工作流程？     | ✅ 是                 | ❌ 否            |\n| 希望Agent直接与用户交互？ | ❌ 否                 | ✅ 是            |\n| 专家之间复杂的、类人对话？   | ❌ 有限                | ✅ 强            |  \n你可以混合使用两种模式——使用 **交接** 进行Agent切换，并让每个Agent **将子Agent作为工具调用** 来执行专门任务。  \n关于工具调用模式的使用请查看 [Agent Tool 文档](https://java2ai.com/docs/frameworks/agent-framework/advanced/agent-tool) 。",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\agent-tool.md\n内容: // 使用 - 主Agent会根据需求自动选择合适的工具\nOptional<OverAllState> result = multiToolAgent.invoke(\n\"请写一篇关于AI的文章，然后翻译成英文，最后给出摘要\");\n```  \n在这种模式中：  \n1. **专业化分工** ：每个子Agent专注于特定领域（写作、翻译、总结等）\n2. **灵活组合** ：主Agent可以根据任务需求调用一个或多个工具\n3. **智能路由** ：主Agent根据工具的描述和用户需求，自动选择合适的工具\n4. **顺序执行** ：主Agent可以按顺序调用多个工具，实现复杂的工作流  \n**提示** ：为每个子Agent提供清晰、准确的 `description` 非常重要，这直接影响主Agent如何选择合适的工具。描述应该简洁地说明Agent的职责和能力。"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "从上下文工程角度看，系统提示、消息历史、工具、模型和响应格式分别起什么作用？",
      "reference": "系统提示定义行为边界，消息历史提供对话连续性，工具决定外部能力，模型决定推理能力，响应格式决定输出是否可结构化处理；这五者共同组成 Agent 的可控上下文。",
      "response": "从上下文工程（Context Engineering）的角度来看，**系统提示、消息历史、工具、模型和响应格式**共同构成了**模型上下文（Model Context）**。它们决定了大语言模型（LLM）在单次调用中看到的内容，直接影响 Agent 的可靠性、行为表现以及运行成本。\n\n具体作用如下：\n\n1.  **系统提示 (System Prompt)**\n    *   **作用**：作为开发者对 LLM 的**基础指令**。它设定了模型的角色、行为准则、约束条件以及处理任务的总体逻辑。\n2.  **消息历史 (Messages)**\n    *   **作用**：发送给 LLM 的**完整消息列表**（即对话历史）。它提供了上下文背景，使模型能够理解当前的请求是基于之前的哪些交互或信息提出的。\n3.  **工具 (Tools)**\n    *   **作用**：定义了 Agent 可以访问以**采取行动**的外部能力（如搜索、计算、API 调用等）。模型通过识别工具定义来决定何时以及如何调用它们。\n4.  **模型 (Model)**\n    *   **作用**：指定要调用的**实际模型及其配置**。这决定了处理请求的核心引擎能力、推理速度以及成本。\n5.  **响应格式 (Response Format)**\n    *   **作用**：模型最终响应的**架构规范**。它约束了输出的结构（例如 JSON Schema），确保输出内容符合预期格式，便于下游程序解析或保证一致性。\n\n**总结**：这些组件属于**瞬态上下文**，即 LLM 在单次调用中看到的临时信息。通过精细控制这些元素，开发者可以从状态（短期记忆）、存储（长期记忆）或运行时配置中动态组装最适合当前任务的上下文。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\context-engineering.md\n内容: ## 模型上下文（Model Context）  \n控制每次模型调用中包含的内容--指令、可用工具、使用哪个模型以及输出格式。这些决策直接影响可靠性和成本。  \n💬  \n系统提示  \n开发者对LLM的基础指令。  \n💭  \n消息  \n发送给LLM的完整消息列表（对话历史）。  \n🔧  \n工具  \nAgent可以访问以采取行动的工具。  \n🧠  \n模型  \n要调用的实际模型（包括配置）。  \n📋  \n响应格式  \n模型最终响应的架构规范。  \n所有这些类型的模型上下文都可以从 **状态** （短期记忆）、 **存储** （长期记忆）或 **运行时上下文** （静态配置）中获取。",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\rag.md\n内容: // 添加系统提示（包含检索到的上下文）\nString systemPrompt = String.format(\"\"\"\n你是一个有用的助手。基于以下上下文回答问题。\n如果上下文中没有相关信息，请说明你不知道。\n\n上下文：\n%s\n\"\"\", context);\nenhancedMessages.add(new SystemMessage(systemPrompt));\n\n// 保留原有的消息\nenhancedMessages.addAll(previousMessages);\n\n// 使用 REPLACE 策略替换消息\nreturn new AgentCommand(enhancedMessages, UpdatePolicy.REPLACE);\n}\n\nprivate String extractUserQuestion(List<Message> messages) {\n// 从消息列表中提取最后一个用户消息\nfor (int i = messages.size() - 1; i >= 0; i--) {\nMessage msg = messages.get(i);\nif (msg instanceof UserMessage) {\nreturn ((UserMessage) msg).getText();\n}\n}\nreturn null;\n}\n}\n\n// 创建带有 RAG Hook 的 Agent\nReactAgent ragAgent = ReactAgent.builder()\n.name(\"rag_agent\")\n.model(chatModel)\n.hooks(new RAGMessagesHook(vectorStore))\n.build();\n\n// 调用 Agent\nAssistantMessage response = ragAgent.call(\"Spring AI Alibaba支持哪些模型？\");\nSystem.out.println(\"答案: \" + response.getText());\n```",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\context-engineering.md\n内容: ### 你可以控制什么  \n要构建可靠的 Agent，你需要控制 Agent 循环每个步骤发生的事情，以及步骤之间发生的事情。  \n| 上下文类型   | 你控制的内容                         | 瞬态或持久   |\n|---------|--------------------------------|---------|\n| 模型上下文   | 模型调用中包含什么（指令、消息历史、工具、响应格式）     | 瞬态      |\n| 工具上下文   | 工具可以访问和产生什么（对状态、存储、运行时上下文的读/写） | 持久      |\n| 生命周期上下文 | 模型和工具调用之间发生什么（摘要、防护栏、日志等）      | 持久      |  \n- 瞬态上下文。LLM 在单次调用中看到的内容。你可以修改消息、工具或提示，而不改变状态中保存的内容。\n- 持久上下文。跨轮次保存在状态中的内容。生命周期钩子和工具写入会永久修改它。"
      ],
      "metricScores": {
        "context_recall": 1.0,
        "faithfulness": null
      }
    },
    {
      "userInput": "为什么 Agent 会失败，问题通常出在什么地方？",
      "reference": "Agent 失败通常不是单次回答能力不足，而是循环中的某一步做错了，比如工具选错、上下文带错、格式不对或中间状态被污染；所以要控制每一步而不是只盯最终答案。",
      "response": "Agent 失败通常是因为其内部的 LLM 调用采取了错误的操作，或者没有按照预期执行。问题主要集中在以下两个核心方面：\n\n1.  **缺乏“正确”的上下文（最常见原因）**\n    大多数情况下，Agent 不可靠是因为没有向 LLM 传递足够的上下文信息。这被称为**上下文工程（Context Engineering）**的问题。如果 Agent 不知道当前环境的状态、可用的工具或任务的具体背景，它就无法做出正确的决策。\n\n2.  **底层 LLM 能力不足**\n    模型本身的推理能力、指令遵循能力或知识储备不足以处理当前复杂的任务。\n\n此外，在具体的工程实现中，还可能遇到基础设施层面的问题，例如：\n*   **Agent 发现失败**：比如配置未开启（`discovery.enabled`）、未成功注册到服务发现中心（如 Nacos）或名称不匹配，导致 Agent 之间无法通信。\n\n**总结**：构建可靠 Agent 的难点在于生产环境的稳定性。解决 Agent 失败问题的首要工作通常是优化**上下文工程**，确保以正确的格式提供正确的信息和工具。",
      "retrievedContexts": [
        "文档 1\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\context-engineering.md\n内容: ### 为什么 Agent 会失败？  \n当 Agent 失败时，通常是因为 Agent 内部的 LLM 调用采取了错误的操作或者没有按我们预期的执行。LLM 失败的原因有两个：  \n1. 底层 LLM 能力不足\n2. 没有向 LLM 传递\"正确\"的上下文  \n大多数情况下 -- 实际上是第二个原因导致 Agent 不可靠。  \n**上下文工程** 是以正确的格式提供正确的信息和工具，使 LLM 能够完成任务。这是 AI 工程师的首要工作。缺乏\"正确\"的上下文是更可靠 Agent 的头号障碍，Spring AI Alibaba 的 Agent 抽象专门设计用于优化上下文工程。",
        "文档 2\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ## 故障排查  \n### Agent 没有注册到 Nacos  \n- 检查 `registry.enabled: true` 是否配置\n- 查看应用日志，确认 Nacos Registry AutoConfiguration 是否生效\n- 验证 Nacos 连接配置（server-addr、username、password）",
        "文档 3\n来源: D:\\PythonProject\\Agentic RAG\\data\\md\\a2a.md\n内容: ### AgentCardProvider 无法发现 Agent  \n- 检查 `discovery.enabled: true` 是否配置\n- 确认 Agent 已成功注册到 Nacos\n- 验证 agent name 是否匹配"
      ],
      "metricScores": {
        "context_recall": 0.0,
        "faithfulness": null
      }
    }
  ]
}